#ifndef CHIDAPI_SHIM_H
#define CHIDAPI_SHIM_H

/* Homebrew hidapi (brew install hidapi). Absolute path avoids SPM relative headerSearchPath limits. */
#include "/opt/homebrew/include/hidapi/hidapi.h"

#endif
